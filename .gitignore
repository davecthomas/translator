node_modules/
dist/
.env
.env.*.local
.env.local
.DS_Store

# Vercel — local linking artifacts that contain account-specific IDs
# (projectId, orgId). Never commit these. The deploy is configured via
# Vercel's web dashboard; see README.md "Deploy to Vercel".
.vercel/

# agentmemory-managed local repo wiring and state
# Added by the agentmemory SessionStart/bootstrap flow.
# Do not edit this block manually; rerun the installer from your agentmemory checkout and reopen an agent session if needed.
.githooks/
.codex/local/
.claude/local/
.claude/settings.local.json
.agents/memory/pending/
.agents/memory/state/
.agents/memory/logs/
.agents/memory/.auto_bootstrap_running
